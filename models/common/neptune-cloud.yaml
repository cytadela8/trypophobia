parameters:
  input_location:
    default: '/input/'
  output_location:
    default: '/output/'
  data_folder:
    description: Path to the folder containing the dataset without preeceding and succeding slashes and without the input location
    default: 'tryponet_set-test'
  number_of_epochs:
    description: Maximum number of epochs
    default: 100
  batch_size:
    default: 64
  resolution_x:
    default: 256
  resolution_y:
    default: 256
tags: [tryponet]
